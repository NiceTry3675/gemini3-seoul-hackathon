# PRD: 9-Cut Teaser Studio (MVP)

> **행사**: Google Gemini 3 Seoul Hackathon (2026-02-28)
> **팀 규모**: 4
> **목표**: 소설 초반 텍스트(또는 줄거리)를 입력하면, 인스타그램 그리드용 **9컷 티저 이미지**를 자동 생성하는 웹앱

---

## 1. 제품 개요

### 1.1 한 줄 소개

소설 초반을 “웹툰 티저”로 재구성해, 말풍선 텍스트가 포함된 9장의 이미지(3x3 그리드)를 생성한다.

### 1.2 MVP에서 반드시 하는 것

- 입력 텍스트 1개로 9컷 티저 생성(고정 9컷)
- 스타일 템플릿(A~D) 선택
- 출력 언어 선택(ko/en/ja, 기본 ko)
- **이미지에 말풍선 텍스트를 모델이 직접 렌더링**
- 캐릭터 일관성 강화를 위한 레퍼런스 이미지 전략
  - 캐릭터 앵커 이미지 1장 생성
  - 1컷 생성: `캐릭터 앵커`를 레퍼런스로 사용
  - 2~9컷 생성: `캐릭터 앵커 + 직전 컷`을 레퍼런스로 사용

### 1.3 MVP에서 하지 않는 것(보류/폐기)

- 12컷 컨티/장면 분해/검증(QA) 파이프라인: **폐기 또는 보류**
- 스포일러 방지 “로직” 구현: **하지 않음**
  - 프롬프트에 “결말 해소/정답 단정 표현은 피하라” 수준의 최소 가이드만 둔다.
- OCR/비전 기반 텍스트 정확도 검증: **보류**

---

## 2. 핵심 결정사항

| 결정 항목 | 선택 | 근거 |
| --- | --- | --- |
| 컷 수 | 9컷 고정 | 인스타 3x3 그리드에 최적, 구현 범위 통제 |
| 텍스트 렌더링 | 이미지 내 직접 렌더링(말풍선) | 데모 임팩트(단, 오타/깨짐 리스크는 감수) |
| 전개 방식 | 고정 규칙 없음 | Gemini의 설계 자율성 부여(과설계 방지) |
| 이미지 비율 | 1:1 고정(MVP) | 그리드에 최적, 구현 단순 |
| 텍스트 모델 | `gemini-3.1-pro-preview` | 9컷 플랜 JSON을 1회에 생성 |
| 이미지 모델 | `gemini-3.1-flash-image-preview` | 텍스트+이미지 레퍼런스 기반 생성 |
| 백엔드 | FastAPI + google-genai + Pydantic | 빠른 MVP, JSON 검증 |
| 프론트 | Next.js 16 + React 19 + Tailwind | 빠른 UI 구성 |

---

## 3. 사용자 플로우 (UI/UX)

1. 텍스트 입력: 소설 초반 텍스트/요약을 붙여넣기
2. 옵션 선택: 언어(기본 ko) + 스타일(A~D)
3. 생성 버튼 클릭: 진행 상태 표시(9컷 순차 생성)
4. 결과: 9장 이미지 그리드 프리뷰 + 개별 다운로드

---

## 4. 데이터 흐름(최소 파이프라인)

1. 텍스트(1회): 9컷 플랜 JSON + 캐릭터 앵커 프롬프트 생성
2. 이미지(1회): 캐릭터 앵커 이미지 생성
3. 이미지(9회): 1~9컷 생성(레퍼런스: 앵커 + 직전 컷)

---

## 5. 리스크/제약(명시)

- 이미지 내 말풍선 텍스트는 모델 특성상 오타/깨짐이 발생할 수 있다(검증 QA는 MVP에서 보류).
- 입력 텍스트가 길거나 복잡하면 9컷이 과밀해질 수 있다(텍스트는 “짧고 굵게” 유도).
